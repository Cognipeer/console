/**
 * LangChain type declarations.
 *
 * All currently-used LangChain packages ship their own .d.ts files.
 * Only add ambient module declarations here for packages that genuinely
 * lack type definitions — empty `declare module` stubs shadow real types
 * and suppress useful type-checking.
 */
export {};
