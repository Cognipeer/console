apiVersion: v1
data:
  tls.crt: "{{.Values.tls_secrets.fullchain_pem}}"
  tls.key: "{{.Values.tls_secrets.privatekey_pem}}"
kind: Secret
metadata:
  name: cognipeer-console-{{.Values.environment}}-tls
  namespace: cognipeer-console-{{.Values.environment}}
type: kubernetes.io/tls