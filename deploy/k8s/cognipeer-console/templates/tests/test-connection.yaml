apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cognipeer-console.fullname" . }}-test-connection"
  labels:
    {{- include "cognipeer-console.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cognipeer-console.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
