apiVersion: v1
kind: Service
metadata:
  name: {{ include "cgate.fullname" . }}
  labels:
    {{- include "cgate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cgate.selectorLabels" . | nindent 4 }}
